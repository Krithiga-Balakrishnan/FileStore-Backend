// const users = []; // In-memory database

// function addUser(user) {
//     users.push(user);
// }

// function checkUserExists(username, email, folderName) {
//     return users.some(
//         (user) => user.username === username || user.email === email || user.folderName === folderName
//     );
// }

// module.exports = { addUser, checkUserExists };
